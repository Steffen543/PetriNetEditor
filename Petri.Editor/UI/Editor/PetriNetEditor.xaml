<UserControl x:Class="Petri.Editor.UI.Editor.PetriNetEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Petri.Editor.UI.Editor"
             mc:Ignorable="d"  x:Name="this"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300px" />
        </Grid.ColumnDefinitions>

        <local:EditorDislay DataContext="{Binding .}"  x:Name="EditorDisplay" Grid.Column="0" />

        <local:EditorTools x:Name="EditorTools" DataContext="{Binding .}" Grid.Column="1" />

        

    </Grid>
</UserControl>
